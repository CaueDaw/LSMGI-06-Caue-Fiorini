let $employees := doc("CLASSICMODELS_BD/Employees.xml")/employees/employee
for $e in $employees
order by number($e/@employeeNumber)
return concat("Nom:", $e/firstName, " ", $e/lastName,
              ", Numero d'empleat:", $e/@employeeNumber,
              ", Oficina assignada:", $e/office/city)